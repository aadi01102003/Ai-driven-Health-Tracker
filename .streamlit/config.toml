[server]
headless = true
address = "0.0.0.0"
port = 5000

[theme]
# Custom theme settings for better mobile visibility
primaryColor = "#90caf9"
backgroundColor = "#1e1e1e"
secondaryBackgroundColor = "#262626"
textColor = "#ffffff"
font = "sans serif"

[browser]
gatherUsageStats = false

[runner]
fastRerenderInterval = 1000  # Better performance on mobile